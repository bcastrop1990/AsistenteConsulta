files:
  "/etc/nginx/conf.d/proxy.conf":
    mode: "000755"
    owner: root
    group: root
    content: |
      client_header_timeout 300s;
      client_body_timeout 300s;
      keepalive_timeout 300s;
      gzip on;